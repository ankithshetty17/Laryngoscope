import 'package:flutter/material.dart';

class VideocallLoginScreen extends StatelessWidget {
  const VideocallLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('helo'),),
    );
  }
}